{
  "name": "sentinel-audit-verifier",
  "version": "0.1.0",
  "private": true,
  "description": "Dependency-free TypeScript verifier for the Sentinel portable audit-chain profile.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node dist/test.js"
  }
}
